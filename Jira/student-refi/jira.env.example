# Jira env example for student-refi templates
JIRA_USER=your-email@example.com
JIRA_TOKEN=your-jira-token-here
JIRA_INSTANCE=https://your-domain.atlassian.net

# Do not commit real credentials. Copy this to `jira.env` locally when needed.
